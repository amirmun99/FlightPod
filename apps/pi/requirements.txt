# FlightPaper Pi service runtime requirements.
# install.sh creates /opt/flightpaper/.venv and installs from this file.
# Hardware extras (gpiozero, spidev, lgpio) are installed conditionally on
# the Pi; they're optional on macOS.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
Pillow>=10.3
PyYAML>=6.0
PyNaCl>=1.5
qrcode>=7.4
